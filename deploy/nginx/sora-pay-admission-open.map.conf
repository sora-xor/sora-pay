# Include exactly one admission map in nginx's http context.
# Opening admission requires the storage and launch checks in docs/relay.md.
map "$request_method:$uri" $sora_pay_admission_blocked {
    default 0;
}
